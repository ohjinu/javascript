var count = 0

var randomNumber = Math.floor(Math.random()*100)+1;

function finding () {
    var userNumber = document.querySelector('#try').value;

    if (userNumber > 0 && userNumber <=100) {
        
    }else {
        alert("1에서 100까지의 숫자를 입력하세요!")
    }
}

